

/*const field_Code = document.querySelector('.field_code');
const code_languagem = document.querySelector('.label_tagCode');
const field_post = document.querySelectorAll('.demoBlock');

function selectedLanguagem() {
    const code = field_Code.querySelector('code');
    field_Code.innerHTML = `<code class="preview hljs ${code_languagem.textContent}" aria-label="editor"></code>`;
    field_Code.firstChild.innerText = code.innerText;
}

window.addEventListener('load', () => {
    selectedLanguagem();
    highlightActive();
})

function highlightActive() {
	const code = field_Code.querySelector('code');
    hljs.highlightBlock(code);
}

function bookmarkOn(){
    const bookmark = document.querySelector('.contentSendFor__iconBookmark__desable');
    bookmark.classList.remove("contentSendFor__iconBookmark__desable");
    bookmark.classList.add("contentSendFor__iconBookmark");
}

function bookmarkOff(){
    const bookmark = document.querySelector('.contentSendFor__iconBookmark');
        bookmark.classList.remove("contentSendFor__iconBookmark");
        bookmark.classList.add("contentSendFor__iconBookmark__desable");
}

function addHighLight(){
    selectedLanguagem();
    highlightActive();
}*/